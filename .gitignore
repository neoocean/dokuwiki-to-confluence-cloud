# Runtime state — sqlite (per-instance, never bundled in deploy)
state.db
state.db-wal
state.db-shm

# Cached intermediates (regenerable by discover/render/convert)
raw/
raw_history/
storage/
storage_history/
storage_struct/
logs/

# Visual audit artifacts
verify-gallery.html
verify-screenshots/

# Container clone artifacts (host data mounted by dev/dokuwiki-local/docker-compose.yml)
dev/dokuwiki-local/bitnami/

# Secrets — NEVER commit
.secrets/

# pytest cache
.pytest_cache/

# Python / venv
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Editor / OS
.DS_Store
.vscode/
.idea/

# Claude Code local state
.claude/

# Operational memory — P4 에는 유지 (운영 메모, 인스턴스별 함정/배운 점),
# GitHub 공개 미러에는 노출 안 시킴
docs/MEMORY.md
MEMORY.md
RESULT.md
RESULT.json

# Visual comparison gallery (regenerable by compare-publish)
compare_screenshots/

# RESULT 생성 스크립트는 일회성 도구 — P4 추적 / GitHub 미공개
_build_result.py
